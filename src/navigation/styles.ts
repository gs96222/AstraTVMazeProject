import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  navContainer: {
    flex: 1,
    width: '100%',
  },
  activityIndicatorView: {
    width: '100%',
    position: 'absolute',
    bottom: 120,
    zIndex: 10,
    alignSelf: 'center',
  },
});
